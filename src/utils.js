export default function getRandomNumber(min = 0, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
export const Emoji = {
  'sleeping': `😴`,
  'neutral-face': `😐`,
  'grinning': `😀`,
};
